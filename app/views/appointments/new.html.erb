
<div class="container">
<h1>Add an Appointment</h1>
<%= form_for [@user, @appointment] do |f|%>
  <%= f.label :user%>
  <%=f.select :user_id, @users.collect { |u| [u.first_name, u.id] }%>
  <br />
  <%= f.label :date %>
  <%= f.datetime_field :date%>
  <br />
  <%= f.submit%>
  <%end%>
  </div>